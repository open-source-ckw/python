PY_ENV=prod # prod | dev | local | or based on file name of additional .env file in /env folder

APP_NAME=ai-python_api_cli
APP_VERSION=0.1.0
DEBUG=false

# Group based envs allow to acces like groupa.url (with single _ its become groupa_url) this behavious is not activated in this project
GROUPA__URL=postgresql+psycopg://user:pass@localhost:5432/appdb
GROUPA__POOL_SIZE=10

GROUPB__URL=redis://localhost:6379/0
GROUPB__ENABLED=true

# General
TZ=America/New_York
PROJECT_NAME=python_api_cli
APP_HOST_DOMAIN=http://localhost:20080
APP_HOST_CDN_DOMAIN=http://localhost:20080
APP_HOST_WEB_DOMAIN=http://localhost:20080
APP_HOST_BACKOFFICE_WEB_DOMAIN=http://localhost:20079
APP_LOCAL_WEB_DOMAIN=localhost
APP_LOCAL_WEB_PORT=20080
APP_LOCAL_BACKOFFICE_WEB_DOMAIN=localhost
APP_LOCAL_BACKOFFICE_WEB_PORT=20079
APP_LISTEN_HOST=localhost

GQL_IS_SUPERGRAPH=false
GQL_DISABLE_SUPERGRAPH_PLAYGROUND=false
GQL_DISABLE_SUBGRAPH_PLAYGROUND=false
GQL_DISABLE_INTROSPECTION=false
GQL_ROOT_SLUG=graphql
GQL_SCHEMA_DIR=/graphql/schema
GQL_LIMIT=200

PASSWORD_LIFE_SPAN_DAYS=365
PASSWORD_RESET_WARNING_DAYS=180
SIGNIN_OTP_EXPIRE_MINUTES=10
FORGOT_PASSWORD_LINK_EXPIRE_HOURS=2
FORGOT_PASSWORD_LINK_SEND_TO_RECOVERY_EMAIL=true  #if false then forgot password link send to primary email

# static storage specification
ONE_BYTE_SIZE=1024
MAX_FILE_SIZE=20971520 # in bytes (20 * 1024 * 1024) = 20 mb
MAX_FILES=10
LOG_DIR=/log/structlog
UPLOAD_DIR=/assets/upload
TMP_DIR=/assets/tmp
IMAGE_DIR=/assets/image
CDN_URL=/cdn

# common secret, salt an iv
COMMON_SECRET=1EB0F2F8B7CF2734283655BFA9FDD73
COMMON_SALT=FA29CAB98
COMMON_IV=74D5350D1A4A493

# stateful data storage
COOKIE_SECRET=7CDFE783DB838E5B6B77540EB8F355
SESSION_SECRET=FCEC7E5412EF1A3CD267432A7
SESSION_SALT=2BABDD1
SESSION_IV=96FD43B8FE7C1A5634

# Database
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASS=root
MYSQL_DBNAME=enterprise_application_db # no db no connection
MYSQL_LOGGING=false # not in use for now
MYSQL_DATABASE_TYPE=#'mysql' # mysql or mariadb # no type no connection

PGSQL_HOST=103.251.16.205
PGSQL_PORT=0000
PGSQL_USER=admin
PGSQL_PASS=xxx
PGSQL_DBNAME=db_name
PGSQL_LOGGING=false # not in use for now
PGSQL_DATABASE_TYPE='postgresql' # no type no connection
PGSQL_SCHEMA='public'

SQLITE_USER=
SQLITE_PASS=
SQLITE_LOGGING=false # not in use for now
SQLITE_DATABASE_TYPE='sqlite' # no type no connection
SQLITE_DBNAME=sqlite.db 
SQLITE_SOURCE_DIR=/data-source

REDIS_HOST=localhost
REDIS_PORT=0000
REDIS_LOGGING=false

DB_POOL_SIZE=10
DB_POOL_MAX_OVERFLOW=10
DB_POOL_PER_PING=true
DB_POOL_USE_LIFO=true
DB_POOL_ECHO=false
DB_POOL_RECYCLE=3600

BETTER_SQLITE3_DATA_SOURCE=/data-source/better.sqlite3.sql

# JWT (make sure when you change below 2 token you must change in /libs/global-app/g-config/g.config.default.ts)
JWT_ACCESSTOKEN_SECRET=F7A0FB9FD88FE52811C6D8C
JWT_REFRESHTOKEN_SECRET=3A237D96597B05C27C30F1E80
JWT_ISSUER=THATSENDISSUER
JWT_AUDIENCE=Application
JWT_ACCESSTOKEN_PAYLOAD_NAME=access_token
JWT_REFRESHTOKEN_PAYLOAD_NAME=refresh_token
JWT_ACCESSTOKEN_EXPIRES_IN=1d # for more formats, visit https://github.com/vercel/ms
JWT_REFRESHTOKEN_EXPIRES_IN=7d

JWT_SIGNING_ALG_ACCESS=RS256 #HS256 # Algorithm for access tokens. Default RS256. Accepts: RS256, RS512, PS256, ES256, EdDSA, HS256, HS512. (Pin per type for safety.) 
JWT_SIGNING_ALG_REFRESH=RS256 #HS256 # Algorithm for refresh tokens. Default RS256.
JWT_PRIVATE_KEY_ACCESS_PATH='' # Filesystem path to private key PEM for access tokens (RSA/EC/Ed25519).
JWT_PUBLIC_KEY_ACCESS_PATH='' # Path to public key PEM for access tokens (recommended for verification).
JWT_PRIVATE_KEY_REFRESH_PATH='' # Private key PEM for refresh tokens.
JWT_PUBLIC_KEY_REFRESH_PATH='' # Public key PEM for refresh tokens.
JWT_KID_ACCESS=v1 # Current key id for access tokens (e.g., v1, 2025-09); appears in JWT header for rotation.
JWT_KID_REFRESH=v1 # Current key id for refresh tokens.
JWT_LEEWAY=30s # Allowed clock skew, e.g., 30s. Applied during verification to exp/nbf/iat. 
JWT_REQUIRE_EXP=true # true|false. If true, exp must exist and is enforced.
JWT_REQUIRE_IAT=true # true|false. If true, iat must exist and is enforced.
JWT_JWKS_ENABLED=true # true|false. If true, you can expose jwt_service.jwks() at /.well-known/jwks.json for public verification.
JWT_JWKS_CACHE_MAX_AGE=3600 # Seconds clients may cache JWKS (e.g., 3600)

# SMTP Variables
SMTP_SERVICE=null
SMTP_HOST=mail.google.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=testsmtp@google.com
SMTP_PASS=xxxx
SMTP_FROM_EMAIL=local@gmail.com

# Data format - datetime.strftime (as per programming language and package used)
FORMAT_DATE_TIME="%-d %b %Y %I:%M:%S %p"
FORMAT_DATE="%-d %b %Y"
FORMAT_TIME="%I:%M:%S %p"

# Pagination
NUM_OF_RECORDS_PER_PAGE=25

# image processing
IMG_THUMB_WIDTH=200
IMG_THUMB_HEIGHT=150
IMG_THUMB_FILE_EXTENSION=.jpeg

# database entity 
ENTITY_PREFIX=te_

# master user
MASTERUSER_USERNAME=admin
MASTERUSER_IDENTIFY=xxx

ENDUSER_USERNAME=enduser@domain.local
ENDUSER_IDENTIFY=xxx
ENDUSER_ROLE=USER

LOG_CONSOLE_LEVEL=DEBUG # Allowed: TRACE, DEBUG, INFO, NOTICE, WARN, ERROR, CRIT, FATAL, ALERT, EMERG
LOG_CONSOLE_FOREIGN_MIN_LEVEL=ERROR # Allowed: CRITICAL, ERROR, WARNING, INFO, DEBUG, NOTSET
LOG_TO_FILES=true
LOG_FILES_PER_PID=true
LOG_KEEP_DAYS=14
LOG_SAMPLE_RATE=1.0 # Float 0.0–1.0 (sampling for low‑level logs)

# ----------------------------------------
# PROJECT SPECIFICATION (third-party)
# ----------------------------------------
